<h1>Profile</h1>
<header>
  <a href="/logout">Logout</a>
  <a href="/">Home</a>
  <a href="/recipes">Recipes</a>
  <a href="/ingredients">Ingredients</a>
</header>
<body>
    <div>
      <h2>Welcome {{user.firstName}}!</h2>
      <p>Here, you can view your profile, liked recipes, and your ingredients</p>
    </div>
    <div>
      <h2>Profile</h2>
      <p>First Name: {{user.firstName}}</p>
      <p>Last Name: {{user.lastName}}</p>
      <p>Username: {{user.username}}</p>
      <p>Display Username: {{user.showUsername}}</p>
      <p>Email: {{user.email}}</p>
      <p>Role: {{user.role}}</p>
    </div>
    <div>
      <h2>Your ingredients</h2>
      <ul>
        {{#each user.ingredients}}
          {{> ingredient}}
        {{/each}}
      </ul>
    </div>
    <div>
      <h2>Recipes made by you:</h2>
      <ul>
        {{#each user.recipesCreated}}
          <li>
            <a href={{this.sourceUrl}}>
              <div>
                {{> recipe}}
              </div>
            </a>
          </li>
        {{/each}}
      </ul>
    </div>
    <div>
      <h2>Liked Recipes:</h2>
      <ul>
        {{#each user.likedRecipes}}
          <li>
            <a href={{this.sourceUrl}}>
              <div>
                {{> recipe}}
              </div>
            </a>
          </li>
        {{/each}}
      </ul>
    </div>
</body>